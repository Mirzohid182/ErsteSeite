import React from "react";
import s from './settings.module.css';

const Settings = (props) => {
    return(
        <div>
                Hello settings
        </div>
    )
}

export default Settings;